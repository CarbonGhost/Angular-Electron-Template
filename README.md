# Angular Electron Template
This is a base Angular project that implements Electron.
## Run for Electron
From the **APP** directory run `npm run electron`.
## Run a Dev Server
From the **APP** directory run `npm start`.
